<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Welcome to HeavenHub</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/particles.js/2.2.2/particles.css">
  <style>
    body {
      margin: 0;
      font-family: 'Dancing Script', cursive; /* Change font to a more whimsical style (optional) */
      background-image: url('https://images.pexels.com/photos/691668/pexels-photo-691668.jpeg?auto=compress&cs=tinysrgb&w=1260&h=750&dpr=2');
      background-size: cover;
      background-position: center;
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      text-align: center;
      overflow-x: hidden;
      color: #fff; /* Set body text color to white (optional) */
    }

    .content {
      transform-style: preserve-3d;
      animation: enterContent 2s ease-in-out forwards;
    }

    @keyframes enterContent {
      0% { opacity: 0; transform: translateY(100px) rotateX(45deg); }
      100% { opacity: 1; transform: translateY(0) rotateX(0); }
    }

    .content h1 {
      animation: textPop 0.5s ease-in-out forwards, textUnderlineGrow 1s ease-in-out forwards;
    }

    @keyframes textPop {
      0% { transform: translateY(-10px); opacity: 0; }
      100% { transform: translateY(0); opacity: 1; }
    }

    @keyframes textUnderlineGrow {
      0% { transform: translateY(0) scaleX(0); }
      100% { transform: translateY(5px) scaleX(1); }
    }

    .explore-btn {
      display: inline-block;
      padding: 10px 20px;
      background-color: transparent; /* Make button transparent */
      color: #fff;
      text-decoration: none;
      font-weight: bold;
      border: 2px solid #fff; /* Add a white border */
      border-radius: 5px;
      transition: background-color 0.3s, transform 0.2s ease-in-out;
    }

    .explore-btn:hover {
      background-color: #ff5a5f;
      transform: scale(1.1); /* Scale button slightly on hover */
    }
  </style>
</head>
<body>
  <div id="particles-js"></div>
  <div class="content">
    <h1 class="animated-text">Welcome to HeavenHub</h1>
    <p>Discover the best lodgings for your next adventure.</p>
    <a href="/listings" class="explore-btn">Explore Destinations</a>
  </div>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/particles.js/2.2.2/particles.js"></script>
  <script>
    particlesJS.load('particles-js', 'particles.json', function() {
      console.log('particles.js loaded - callback');
    });
  </script>
</body>
</html>
